# Backend API base URL. EXPO_PUBLIC_ vars are inlined into the app bundle at
# build time (there's no server to keep this secret behind, unlike the web
# app's BFF) — this is just a hostname, not a credential, so that's fine.
EXPO_PUBLIC_API_URL=http://localhost:3000
